import { EnrichmentInterceptor } from './enrichment.interceptor';

describe('EnrichmentInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichmentInterceptor()).toBeDefined();
  });
});
